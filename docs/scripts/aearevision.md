# `aearevision`

[Source](https://github.com/AEADataEditor/editor-scripts/blob/main/aearevision)

Parses `REPLICATION.md` and converts every `REQUIRED` tag into a `[We REQUESTED]`
tag, ready for a replicator working on a revision report.
