# Maintenance

Standalone administrative scripts. These are **not** installed via `pip`; run
them with `python3` from a checkout of this repository.

- [`jira_workflow_cleanup.py`](jira_workflow_cleanup.md) — archive and delete
  inactive Jira Cloud workflows.
